name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        env:
          TARGET_URL: https://ktuvit-stremio-addon.onrender.com/manifest.json
        run: curl -sf "$TARGET_URL" || true
